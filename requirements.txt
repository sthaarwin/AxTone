# Python 3.11.x REQUIRED (for compatibility)

# Core dependencies
numpy>=1.24.0
scipy>=1.11.0
librosa>=0.10.1

# MIDI handling
mido>=1.3.0
pretty-midi>=0.2.10

# Visualization
matplotlib>=3.8.0

# API Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Build tools
setuptools>=65.0.0

# Optional — ML-based pitch detection (uses ~500 MB RAM, not suitable for free-tier hosting)
# Uncomment locally if you want to use --method basic_pitch
# basic-pitch>=0.3.0
# tensorflow>=2.13.0